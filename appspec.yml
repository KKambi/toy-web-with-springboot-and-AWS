version: 0.0    # CodeDeploy version  (0.0 이외의 버전은 오류)
os: linux
files:
  - source: /   # CodeDeploy 에서 전달받은 파일 중, destination 으로 이동시킬 대상 지정
    destination: /home/ec2-user/app/step2/zip/
    overwrite: yes